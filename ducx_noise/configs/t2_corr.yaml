# Table 2: distractor_10 + description corruption (N_dist + N_corr).
seed: 0
shuffle_tools: true
label: t2_corr
distractor: {enabled: true, count: 10, llm_generate: false, cache_path: ducx_noise/cache/distractors.json, style: plausible}
description_corruption: {enabled: true, level: 0.6, style: misleading, llm_generate: false, cache_path: ducx_noise/cache/descriptions.json}
unreliable: {enabled: false}
redundant: {enabled: false}
schema_noise: {enabled: false}
